import 'package:flutter/material.dart';

class PlayerPosition {
  final Color color;
  final String nameCode;
  final Color textColor;

  PlayerPosition(this.color, this.nameCode, [this.textColor = Colors.black]);
}